# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Environment secrets — never commit these
.env
client/.env
server/.env
client/.env.local

# Build output
client/dist/

# User data — contains password hashes, keep off git
server/data/users.json

# OS
.DS_Store
Thumbs.db
